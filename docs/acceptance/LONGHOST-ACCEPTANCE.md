# LONGHOST-ACCEPTANCE

Generated by `scripts/longhost-acceptance.cjs` from the harness reports in this directory. Every number
below is measured by a program in this run; the file names it came from are in `docs/acceptance`.

* commit: `f00961957fe887ae4cfee871cb0fe71800575a94`
* branch: `try-auto`
* at: 2026-09-17T07:27:56.274Z

## Metrics

| metric | value | evidence |
| --- | --- | --- |
| lostTasks | 0 | chaos kill-core and host-restart keep the task file and the recorded steps (15 checks); soak maintenance/budget never drop a task |
| duplicateTasks | 0 | continuity resumes only what it parked (its own test) and git-interruption shows no duplicate commit |
| duplicateSideEffects | 0 | the restart lock admits one executor; the plugin adopts the companion record instead of writing a second one |
| falseSuccess | 0 | chaos false-success (7 checks): a failed restart is FAILED, a process-only recovery is PROCESS_ONLY, and no task state invents SUCCESS |
| unrecoverableCoreCrashes | 0 | chaos kill-core: the real companion relaunched the killed process and the record reached a terminal phase (15 checks) |
| infiniteRestartLoops | 0 | soak budget: the ladder reaches SAFE_MODE and stops; chaos controlled-restart stays inside the budget |
| repositoryCorruption | 0 | chaos git-interruption: HEAD readable, tree clean after the stage, no blind replay |
| pluginIsolationFailures | 0 | chaos plugin-crash (7) and plugin-timeout (5): the runtime answers for every other plugin and bounds a hook that never returns |
| restartRecoveryFailures | 0 | chaos controlled-restart: the documented order runs and the record carries process/task/semantic recovery |
| intendedUiUnreachable | 0 | the UI repair closed the five high findings; the audit file records what was reachable before |
| duplicatePluginRegistrations | 0 | the installer's registration probe read 0 plugins with no duplicate id |
| unexpectedDeletedFeatures | 0 | no shipped module was deleted: the cleanup merged duplicates and repaired hidden surfaces, and every removal candidate was classified E or F and kept |
| deferredTaskLoss | 0 | work admission holds a task in the queue with a reason instead of failing it, and the scheduler republishes waitingHuman/deferred |

## Verdicts

| judgement | result | evidence |
| --- | --- | --- |
| processLevelUnattendedHosting | PASS | chaos kill-core: a real supervised child was killed with taskkill /F and the real companion relaunched it, with the record reaching a terminal phase |
| pluginLevelRecovery | PASS | chaos plugin-crash: the runtime answered for every plugin while one threw, and the supervisor kept serving; the manager bounds a hook that never answers |
| taskLevelRecovery | PASS | chaos host-restart: the resume intent was written before the restart and the work continued exactly once afterwards |
| semanticTaskContinuation | PASS | chaos controlled-restart: recovery is judged as FULL only when the task half resumed *and* the continuation named where it started from (the checkpoint) |
| uiFallbackAvailability | PASS | the official Settings page draws the plugin roster, the restart record, the advanced policy and the product actions; the five high UI findings in the audit are closed |
| longTermUnattendedCodingReadiness | PASS | soak 120/120 and chaos 65/65 passed in this run; the gates and the installer probe are green |

## What was NOT exercised

* A real Windows reboot: the resume path a reboot drives is exercised (chaos host-restart), but no unattended run reboots the machine, so "DS-Hns auto-starts after a real reboot" is NOT verified here.
* The real-machine 24-hour wall-clock soak: `node scripts/longhost-soak.cjs --realtime --hours 24` is the entry point and `--realtime --smoke` proves its wiring, but 24 hours of wall clock were not waited out.
* The official UI driven by a real browser: it is verified through the client bundle rendered on the React stand-in (DOM-level) plus the host’s own service records; no browser automation is installed in this checkout.

## Evidence files

* `docs/acceptance/installer-registration-report.json`
* `docs/acceptance/longhost-chaos-report.json`
* `docs/acceptance/longhost-soak-report.json`
* `docs/acceptance/ui-surface-audit.json`
