{
  "name": "ds-harness",
  "private": true,
  "version": "0.4.0",
  "description": "DS-Harness: DeepSeek Harness desktop client (Alien shell + Mega dispatch center, customizable ringtones)",
  "main": "desktop-main.cjs",
  "scripts": {
    "start": "electron .",
    "desktop": "electron desktop-main.cjs",
    "smoke": "electron . --smoke-test",
    "check": "node --check desktop-main.cjs"
  },
  "dependencies": {
    "@deepseek-ai/dsh": "0.1.2-rc.1"
  },
  "devDependencies": {
    "electron": "43.4.0"
  }
}
