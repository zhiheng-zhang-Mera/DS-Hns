# Native picker acceptance input isolation

Candidate9900555 full run2026-09-23T04-54-17-635Z-e4d6a1e8 completed FAIL with two failed gates. Unit failures originated in a3099 collision with a concurrent cleanroom UI;28/28 unchanged focused tests passed after stopping that instance. This diagnostic rerun does not replace the failed full suite. Architecture, combined acceptance, storage/process audit and evidence consistency passed in that failed run.

The automatic UI gate was deliberately terminated after its native-picker helper sent the local artifact path into a different qualification conversation. Source inspection established broad-title AppActivate followed by delayed global SendKeys with no per-input ownership check. The unintended second request began directory inspection; both owned acceptance instances were stopped. The original marker-only provider response and the subsequent contaminated state remain separate screenshots under D:\Hns-Cleanroom-Qualification-9\visual-evidence. No independent QA agent was used.

The repair removes global desktop input entirely. The machine gate writes an exclusive native-picker-request.json inside its isolated data directory, then clicks the real picker in its CDP-selected renderer. An operator must inspect and select the correct OS dialog and supply the fixture directory through targeted real input. The machine gate still requires catalog growth, the named installed skill, and actual disk output. No source-channel fallback or warning may stand in for the native install. A120-second operator coordination budget is not a product performance measurement or a lowered acceptance criterion.

Regression executes the actual helper with real filesystem effects and a blocked process-input boundary. The old implementation failed UNSCOPED_DESKTOP_INPUT_FORBIDDEN; repaired behavior passed. Additional cases preserve existing request evidence without opening another dialog and retain an awaiting-input status when the renderer disconnects. Focused picker/readiness4/4 passed. Pinned Node24.14.1 full unit regression completed exit0:1925tests,1923pass,0fail,2repository-defined absent-sample skips,445836.6818ms. Raw output: D:\Hns-Final-Qualification\native-picker-full-unit.log. This is precommit regression, not a replacement for immutable full qualification or real native-picker input evidence.

Reproduction: run the complete qualification runner; watch its run runtime/electron-data/native-picker-request.json when electron-ui-acceptance starts. Use targeted native input to select the recorded directory within120seconds. Do not overlap another real UI journey. The request file is coordination only; it cannot certify PASS. A new immutable full local run, fresh cleanroom and exact-SHA CI remain required after this repair.
