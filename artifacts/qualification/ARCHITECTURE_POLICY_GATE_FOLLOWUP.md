# Architecture policy gate follow-up

Candidate `5cec02512898e9bdc1448fbfe09c6fdc126e8d1c`, tree `ffe2728b8ace5ac86e8f5d80c1718c245c7c7803`, full construction run `2026-09-23T22-11-36-452Z-ecfe3ed7` failed one mandatory gate. It completed at 2026-09-23T22:39:36.525Z: 16/17 passed, architecture verifier failed, evidence consistency PASS. Both full unit invocations passed 1989/1991 with zero failures and two declared absent optional community-sample skips. UI131, chaos65, combined52 and post-test process/storage audits passed within this overall failed run; none makes it final qualification.

Raw evidence remains under `D:/Hns-Final-Qualification/q5cec025-r1/qualification-runs/2026-09-23T22-11-36-452Z-ecfe3ed7/`. The actual native directory picker was completed with screenshot-backed Windows input, with operator-picker-01 through03 outside the immutable raw directory. Accessibility set-value returned an unavailable cached index; fresh screenshot observation and targeted literal typing completed the selection. No global/private input helper was used.

Root cause: `scripts/verify.ps1` still required `healthSchedulerPlugin()` after the effective-policy fix correctly changed the factory call to `healthSchedulerPlugin({ config: options.healthConfig })`. The stale source predicate rejected the repaired wiring while accepting a regression to constructor defaults. This was a missed validation-script update, not evidence that the live-policy fix had failed.

The new unit regression executes the actual PowerShell predicate on the actual mounted source and five controlled mutations. RED: four passed, two failed (correct wiring falsely rejected; default-only Health falsely accepted). Updated predicate requires both existing factories with their respective owner policies and the Restart host/node/state-directory arguments. GREEN: all six passed. Missing factories, discarded policies and swapped Health policy remain rejected. This is bounded gate-validation evidence, not a replacement for the full verifier or renewed immutable-candidate qualification.

The separate real construction GUI diagnostic `visual-diagnostic-5cec025-r1` observed enable, Apply16000 and Check returning `sampling every16000ms`, healthy yes. Its profile was reused and is explicitly not cleanroom. No production baseline, real reboot or real24h is certified here.

Post-fix full `scripts/verify.ps1` (no skip flags) exited0: ALL CHECKS PASSED, 1997 unit tests,1995 passed,0 failed,2 declared optional-sample skips,459089.4651ms. Raw `D:/Hns-Final-Qualification/architecture-policy-full-regression-r1.log`; follow-up process/C audits PASS in `architecture-policy-post-test-audit.json`. The failed prior run's61 indexed files were independently rehashed with0 mismatches. A new frozen-candidate full qualification and cleanroom are still required.
